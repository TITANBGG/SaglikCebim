# SağlıkCebim — Yapılacaklar Listesi
**Tarih:** 02.06.2026  
**Durum:** Sunum hazırlık süreci

---

## ✅ Bugün Tamamlananlar

- [x] Acil durum tespiti %20 → %100 düzeltildi (`clinical_roadmap_engine.py`)
- [x] Türkçe karakter normalizasyonu eklendi (ğ→g, ü→u, ş→s)
- [x] Streaming portu 8001 → 8000 düzeltildi (`useStreamingRoadmap.ts`)
- [x] Radyoloji ısı haritası hardcoded URL düzeltildi (`Radyoloji.tsx`)
- [x] JWT token terminale basılması kaldırıldı (`dependencies.py`)
- [x] Chatbot penceresi mobile responsive yapıldı (`HealthChatBot.tsx`)
- [x] Anamnez profil kayıt sessiz hatası düzeltildi (`Anamnez.tsx`)
- [x] 7 test hatası düzeltildi → 123 test geçiyor
- [x] SECRET_KEY fallback kaldırıldı, startup'ta RuntimeError fırlatıyor
- [x] `datetime.utcnow()` → `datetime.now(timezone.utc)` (8 dosya)
- [x] Pydantic v1 `class Config` → `model_config = ConfigDict` (auth.py)
- [x] Upload/parse endpoint'lerine rate limiting eklendi (reports, radiology)
- [x] CORS `allow_methods` ve `allow_headers` sıkılaştırıldı
- [x] `evidence/test` endpoint'ine auth eklendi
- [x] Bildirim stub endpoint'leri kaldırıldı (main.py'den de çıkarıldı)
- [x] Dead code temizlendi (`_build_findings`)
- [x] Deprecation uyarısı sayısı 308 → 62'ye düştü

---

## 🔴 KRİTİK — Kalan

### 1. `.env` Dosyası Git Geçmişinden Silinmeli
- **Dosya:** `backend (1)/.env`
- **Sorun:** Secret key, DB şifresi, VAPID key repo'da görünüyor
- **Yapılacak:**
  ```bash
  git filter-repo --path .env --invert-paths
  # veya
  git filter-branch --force --index-filter "git rm --cached --ignore-unmatch .env" HEAD
  ```
- `.gitignore`'un `.env`'i kapsadığını doğrula

### 2. ~~Default Secret Key Kaldırılmalı~~ ✅ Tamamlandı

---

## 🟡 ORTA — Kalan

### 3. ~~`datetime.utcnow()` Değiştirilmeli~~ ✅ Tamamlandı

### 4. ~~Pydantic v1 Config Syntax~~ ✅ Tamamlandı

### 5. ~~Upload Endpoint Rate Limiting~~ ✅ Tamamlandı

### 6. ~~CORS Sıkılaştırma~~ ✅ Tamamlandı

### 7. ~~Bildirim Stub Endpoint'leri~~ ✅ Kaldırıldı

### 8. ~~`evidence/test` Auth~~ ✅ Tamamlandı

---

## 🟢 DÜŞÜK — Sonraki Sprint

### 9. Token Refresh Mekanizması
- **Durum:** Şu an sadece access token var, expire olunca login zorunlu
- **Yapılacak:** Refresh token + token rotation ekle

### 10. Dead Code Temizliği
- **Dosya:** `app/api/v1/radiology.py` satır 64-65
- **Sorun:** `_build_findings()` fonksiyonu kullanılmıyor (`pass` ile boş)
- **Yapılacak:** Sil

### 11. Database Migration Stratejisi
- **Sorun:** Sadece 1 Alembic migration var, şema `init_db()` ile oluşturuluyor
- **Yapılacak:** Her model değişikliği için migration yaz, `init_db()` yerine alembic kullan

### 12. MIME Type Validation — File Upload
- **Dosyalar:** `app/api/v1/radiology.py`, `app/api/v1/reports.py`
- **Sorun:** Sadece uzantı kontrol ediliyor, magic byte check yok
- **Yapılacak:** `python-magic` kütüphanesi ile gerçek MIME doğrulama

---

## 📋 Sunum Demo Hazırlığı

> **ÖNEMLİ:** Sunum kullanıcısı hesabına önceden şunları ekle:
> - [ ] Anamnez profili (yaş, cinsiyet)
> - [ ] En az 1 kronik hastalık
> - [ ] En az 1 tahlil raporu (PDF)
> - [ ] Test et: "baş ağrım var" → klinik yol haritası çıkmalı
> - [ ] Test et: "göğsümde baskı var, sol koluma yayılıyor" → 112 yönlendirmesi çıkmalı

---

## 📊 Test Durumu

```
Son çalıştırma: 02.06.2026 (güncel)
Sonuç: 123 passed, 2 skipped
Başarısız: 0
Deprecation uyarısı: 62 (önceki: 308)
```

**test_runner.py notu:** Bu test canlı sunucu + önceden girilmiş hasta verisi gerektirir.  
`python -m pytest tests/test_runner.py` ile ayrıca çalıştır.

---

## ⚠️ Bilinen Sınırlamalar (Sunum için "geliştirme aşamasında" de)

| Özellik | Durum |
|---------|-------|
| Push bildirimleri | Stub — çalışmıyor |
| ClinicalKey entegrasyonu | Demo cookie ile çalışıyor |
| UpToDate entegrasyonu | Lisans gerektiriyor |
| Radyoloji AI | PyTorch yüklü değilse fallback |
| Mobil sidebar | Desktop odaklı tasarım |
